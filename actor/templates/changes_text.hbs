<p>
{{#each sitemaps}}
    <ul class="list">
        {{#each this.newUrls}}
            <li>NEW | {{this}} | {{printDate ../../previouslyCrawledAt}} | {{printDate ../../crawledAt}} | {{printChangedDate ../../changeDetails ../../crawledAt this}}</li>
        {{/each}}
        {{#each this.removedUrls}}
            <li>REMOVED | {{this}} | {{printDate ../../previouslyCrawledAt}} | {{printDate ../../crawledAt}} | {{printChangedDate ../../changeDetails ../../crawledAt this}}</li>
        {{/each}}
        {{#each this.lastModifiedChangedUrls}}
            <li>CHANGED | {{this}} | {{printDate ../../previouslyCrawledAt}} | {{printDate ../../crawledAt}} | {{printChangedDate ../../changeDetails ../../crawledAt this}}</li>
        {{/each}}
    </ul>
{{/each}}
</p>
